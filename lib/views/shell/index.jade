!!!
html
  head
    title= title
    link(rel='stylesheet', href="#{baseUrl}/stylesheets/harness.css")

    script(type='text/javascript', src='/core/libs.js')
    script(type='text/javascript', src='/build/client-min.js')

  body
    .harness.shell
      .left
        != partial('header')
        .region.body
        != partial('footer')
      .main
        != partial('header')
        .region.body
          != partial('main_default')
        != partial('footer')

    :coffeescript
      foo = require 'foo'
      console.log 'foo', foo

      console.log 'Libs:'
      console.log '  $', $
      console.log '  _', _
      console.log '  _', _.each([1,2,3], (i) -> console.log 'i: ', i )
      console.log '  _.string', _.endsWith
      console.log 'Backbone', Backbone

