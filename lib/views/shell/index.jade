!!!
html
  head
    title= title
    link(rel='stylesheet', href="#{baseUrl}/stylesheets/harness.css")
    
    
    != html.script('/core/libs.js')
    != html.script('/core/core.js')
    != html.script(baseUrl + '/javascripts/harness.js')
  

  body
    .harness.shell
      .left
        != partial('header')
        .region.body
        != partial('footer')
      .main
        != partial('header')
        .region.body
          != partial('main_default')
        != partial('footer')

    :coffeescript
      harness = require('harness/app')
      harness.init()
